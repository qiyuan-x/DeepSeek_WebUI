version: '3.8'

services:
  deepseek-webui:
    build: .
    container_name: deepseek-webui
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
